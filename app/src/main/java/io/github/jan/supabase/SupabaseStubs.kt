package io.github.jan.supabase.stub

// Minimal placeholder stub moved to a different package so the real Supabase library will be used when available.

class Postgrest
class Gotrue

fun createSupabaseClient(supabaseUrl: String, supabaseKey: String, block: Any.() -> Unit): Any {
    // stub no-op
    return Any()
}
